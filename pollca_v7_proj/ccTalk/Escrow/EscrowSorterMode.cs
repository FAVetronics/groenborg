namespace ccTalk
{
    /// <summary>
    /// Escrow sorter modes for master setup.
    /// </summary>
    public enum EscrowSorterMode
    {
        /// <summary>Escrow locked - don't use!.</summary>
        Locked = 0,
        /// <summary>Escrow - default mode.</summary>
        Escrow = 1,
    }
}